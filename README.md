Aplicación que muestra usuarios usando php y mysql

La aplicación muestra una tabla de los usuarios guardados en la DB.


Para crear los contenedores:
 - Accedemos al directorio dockercc
 
 - docker-compose up --build

 - Para crear la tabla y sus usuarios hace falta ejecutar install.php. http://<host>/install.php
